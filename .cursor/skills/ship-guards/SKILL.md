# ship-guards

Evaluate /ship pipeline guards. Dispatches to qa-resilience-game and prod-readiness-game rule engines, cook-package-game, and cert-game for phase-boundary enforcement.

## MCP tools

- `ship_guard_evaluate` — dispatch a single ship guard by id; see `tools/ship_guard_evaluate.py`.

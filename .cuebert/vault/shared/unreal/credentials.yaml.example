# Unreal Engine paths and Remote Control API placeholder.
# Copy to credentials.yaml and fill in real values.
unreal:
  engine_path: "/path/to/UE_5.x"
  remote_control_host: "http://localhost:30010"
  remote_control_port: 30010
  build_mode: "dry_run"  # Set to "live" when engine is available
